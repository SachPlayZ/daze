# Telegram boundary

Allowed commands: `/start`, `/link`, `/today`, `/team`, `/points`, `/settings`, `/unlink`, `/stop`.

This process sends direct messages from committed ledger rows only. It must never connect/sign wallets, accept stakes, edit/lock teams, claim prizes, disclose full wallets, or send personal points to group chats.

